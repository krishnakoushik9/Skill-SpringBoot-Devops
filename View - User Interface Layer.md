### **What it does**:

- Represents **what the user sees**: interface, layouts, and graphics.
    
- Displays data from the model and sends user interactions to the controller.
    

### 🧠 **Key Features**:

- Focuses on **user experience (UX)** and **user interface (UI)**.
    
- It does **not process data**, but only **presents it**.
    
- Can have dynamic data rendered by templates or components.
    

### 🧰 **Examples**:

- **HTML/CSS**, **JavaScript**, **Bootstrap**, **Tailwind CSS**
    
- UI Frameworks: **React, Angular, Vue.js, Flutter (for apps)**
    
- In web MVC frameworks, views often use template engines:
    
    - **EJS / Handlebars (Node.js)**
        
    - **Jinja2 (Python/Django)**
        
    - **Thymeleaf (Spring)**