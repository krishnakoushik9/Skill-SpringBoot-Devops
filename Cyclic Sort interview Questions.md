- When given Numbers from range 1 to n we need to use Cyclic Sort
- When you see:
- Array contains elements `from 1 to n` or `0 to n-1`
- You're asked to solve in **O(n) time** and **O(1) space**
👉 Think **Cyclic Sort** immediately.


### 1. **Leetcode 268 – Missing Number** (🔗 [Link](https://leetcode.com/problems/missing-number/)) Amazon Interview Question

> Given an array `nums` containing `n` distinct numbers in the range `[0, n]`, return the one number that is missing.
> The answer is at [[Missing Number 268]]

### 2. **Leetcode 448 – Find All Numbers Disappeared in an Array** Google Interview Question(🔗 [Link](https://leetcode.com/problems/find-all-numbers-disappeared-in-an-array/))

> Given an array of integers where `1 ≤ a[i] ≤ n` (n = size of array), return all the elements that **do not appear** in the array.[[Find All Numbers Disappeared in an Array - LeetCode]]

### 3. **Leetcode 442 – Find All Duplicates in an Array** (🔗 [Link](https://leetcode.com/problems/find-all-duplicates-in-an-array/))

> Given an array of integers where `1 ≤ a[i] ≤ n` (n = size of array), some elements appear **twice** and others once. Return all duplicates. [[Find All Duplicates in Array Google Interview Question]]

### 4. **Leetcode 41 – First Missing Positive** (🔗 [Link](https://leetcode.com/problems/first-missing-positive/)) 🧠 _Hard_

> Given an unsorted integer array, find the smallest missing **positive** integer in **O(n)** time and **constant space**. Answer : [[First Missing Positive Number]]

### 5. **Leetcode 287 – Find the Duplicate Number** (🔗 [Link](https://leetcode.com/problems/find-the-duplicate-number/)) 🧠 _Hard_

> Given an array `nums` containing `n + 1` integers where each integer is between `1` and `n` (inclusive), return the duplicate. **Only one duplicate exists** but it may be repeated multiple times.

---
## 🌱 2 **Very Easy** Practice Questions

These help build intuition for cyclic sort:

### 6. **Custom Easy Q1:**

> Given an array of `n` elements where each element is in the range `0 to n-1`, sort the array using cyclic sort.  
> Example: `arr = [3, 0, 1, 2]` → Output: `[0, 1, 2, 3]`

### 7. **Custom Easy Q2:**

> Given an array of size `n` containing numbers `1 to n` (in random order, no duplicates), sort the array **in-place** in O(n) using cyclic sort.